import { Typography } from '@mui/material';

const Orders = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Orders Page
      </Typography>
      <Typography variant="body1">This is the Orders page.</Typography>
    </div>
  );
};

export default Orders;
